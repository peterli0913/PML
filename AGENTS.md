# AGENTS.md

## Cursor Cloud specific instructions

This repository ("PML") is currently an empty, freshly initialized repo with only a `README.md`.

- **No source code, dependencies, or services exist yet.** There is nothing to build, lint, test, or run.
- **No update script is needed** until actual project files (e.g. `package.json`, `requirements.txt`, `Cargo.toml`, etc.) are added.
- Once the codebase is populated, this section should be updated with:
  - How to install dependencies
  - How to run lint, tests, and the dev server
  - Any non-obvious caveats discovered during setup
